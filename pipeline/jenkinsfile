pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                sh 'robot test/test.robot'
            }
        }
    }
    post {
        always {
            junit 'output.xml'
        }
    }
}